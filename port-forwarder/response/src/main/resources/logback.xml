<?xml version="1.0" encoding="UTF-8"?>
<!--
    forwarding / response 模块日志配置（Logback）
    =======================================================================
    历史归档文件自动打包为 .zip 压缩格式，节省磁盘空间。
-->
<configuration>
    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-response}"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
        </encoder>
    </appender>

    <!--
        FILE：滚动文件追加器（RollingFileAppender）
        - fileNamePattern 以 .zip 结尾，Logback 会在滚动时自动将归档文件压缩为 ZIP 格式。
        - 同一天内若当前文件超过 maxFileSize，则 %i 递增生成 response.yyyy-MM-dd.0.log.gz、.1.log.gz …
    -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} -- %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>500MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>20GB</totalSizeCap>
            <cleanHistoryOnStart>false</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <logger name="org.wlpiaoyi.framework.forwarding" level="DEBUG"/>
    <logger name="org.wlpiaoyi.framework.utils.socket" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
